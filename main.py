from Vector2 import Vector2
import VectorDraw
from Vector2 import Vector2

VectorDraw.InitializeCanvas(800, 800)

VectorDraw.DrawLine(Vector2(0, 0), Vector2(200, 200))
VectorDraw.DrawLine(Vector2(0, 100), Vector2(200, 200))

VectorDraw.EndDrawing()
